using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ThemeManagerItem : MonoBehaviour
{
    // Start is called before the first frame update
    public Themes themes;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
